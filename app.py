import streamlit as st

# إعداد واجهة الموقع وتوجيه النص للعربية
st.set_page_config(page_title="محلل الحقيقة العلمي", layout="wide")

st.markdown("""
    <style>
    .main { direction: rtl; text-align: right; }
    </style>
    """, unsafe_allow_html=True)

st.title("🛡️ أداة تحليل المصداقية العلمية")
st.write("أداة تحليل المنشورات بناءً على المنهج الأكاديمي والمنطق الرصين.")

url = st.text_input("أدخل رابط المنشور المراد تحليله:")

if st.button("بدء التحليل"):
    if url:
        st.success("تم جلب البيانات بنجاح!")
        st.subheader("📊 تقرير الفحص الأولي")
        st.metric("مؤشر الموثوقية", "85%")
        st.info("**المستفيد الأكبر:** المجتمع العلمي العام.")
    else:
        st.error("يرجى إدخال الرابط أولاً.")
